{
  "name": "npm-package-json-lint-config-auto",
  "version": "1.0.0",
  "description": "NPM Package.json Lint Config",
  "main": "./.build/index.js",
  "author": "Connor White",
  "license": "MIT",
  "private": false,
  "files": [
    "./.build/**/*.js",
    "./.build/**/*.d.ts"
  ],
  "scripts": {
    "build": "swc source -d .build && tsc --project ./tsconfig.json --incremental --emitDeclarationOnly"
  },
  "devDependencies": {
    "@autosoft/tsconfig": "*"
  },
  "dependencies": {
    "npm-package-json-lint": "^6.4.0"
  }
}
